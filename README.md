# math-autoresearch (`mar`)

数学の未解決問題に対して **探索 → 有限証明書 → 独立検証 → 日本語論文 (LaTeX/PDF)**
を一本のコマンドで回すための研究自動化パイプライン。

```
python -m mar list                       # 問題一覧
python -m mar new p0005_xxx "タイトル"   # 新しい問題の雛形 (2 ファイル) を作る
python -m mar survey  p0000_jacobian_2026   # 先行研究ゲート (未解決性の根拠)
python -m mar search  p0001_xxx --budget 600
python -m mar verify  --all              # 全証明書を独立に再検査
python -m mar paper   p0001_xxx          # 日本語 PDF を生成
python -m mar run     p0001_xxx          # search → verify → paper
python -m mar announce --all             # 成果の投稿文 (既定は下書き表示のみ)
```

## 設計原則

1. **主張は有限証明書に落とす。** 結果とは「対象 (JSON)」と「検証手続き」の 2 つ組。
   浮動小数点数は証明書に入れない (整数・有理数・代数的数のみ)。
2. **検証器は探索器と実装を共有しない。** 探索は SymPy / NumPy を使ってよいが、
   検証は `mar.exact` (標準ライブラリの `Fraction` だけで書いた多項式環・剰余環) を使う。
   共有バグが検証をすり抜けるのを防ぐため。
3. **先行研究ゲート。** `Problem.survey` に「この日付時点で未解決」と言える出典が
   なければ探索を実行しない (`mar search` が拒否する)。計算探索の最大の失敗は
   誤りではなく *既知の結果の再発見*。
4. **検証に落ちた証明書は論文化しない。** `mar paper` は既定で verify を
   再実行し、1 つでも FAIL があれば PDF を作らない。検証結果は
   証明書のダイジェストと**検証器コードのハッシュ**を添えて
   `data/verifications/` に記録する。全数走査に数十分かかる問題では
   `mar paper --use-record` で直前の記録を再利用できるが、証明書か検証器を
   書き換えていれば記録は無効になり、検証はやり直される。
5. **数値は本文に直書きしない。** 論文本文は証明書 JSON から生成する
   (`paper_sections`)。人手で書いた数字と実際の計算結果がずれる事故を構造的に防ぐ。

## ディレクトリ

```
src/mar/
  certificate.py     証明書 (JSON) と再現メタデータ
  exact.py           検証専用の厳密演算 (多変数多項式環, QQ[z]/(g), 無平方判定)
  problem.py         Problem インタフェース + レジストリ
  __main__.py        CLI
  problems/          1 問題 1 モジュール (pNNNN_*.py), 本文は _pNNNN_paper.py
  search/            探索エンジン (グラフ列挙, 不変量, 再現可能な証人書き出し)
  report/            LaTeX 生成 (jlreq + luatexja + 原ノ味フォント, lualatex)
  announce/          成果の告知 (X 投稿文の組み立て・投稿台帳・API クライアント)
data/certificates/   証明書 JSON
data/witnesses/      証人のサイドカー (バイナリ)
data/verifications/  検証記録 (証明書ダイジェストつき。paper の再検証省略に使う)
data/announce/       投稿台帳 (どの成果をどの証明書の版で投稿したか)
papers/<problem_id>/ main.tex / main.pdf / preview/*.png
tools/pdf2png.py     組版の目視確認用
tools/announce_schedule.py  1 日 1 件だけ投稿する Windows タスクの登録
docs/methodology.md  研究自動化の方法論と、実際に踏んだ落とし穴
tests/test_tamper.py     改竄した証明書が検証で落ちることの回帰テスト
tests/test_hand_proofs.py 論文本文の手証明を元データの総当たりで照合
tests/test_witness_repro.py 証人サイドカーがバイト単位で再現可能であること
```

```
PYTHONPATH=src python -m pytest tests/ -q     # 数十秒 (小さい族に縮めて走らせる)
```

テストが 2 本に分かれているのは、守る対象が違うため。**正しい証明書が通ることは
検証器が何かを検査している証拠にならない** (何も検査しない検証器でも通る) ので、
`test_tamper.py` は証明書と証人を実際に書き換えて対応する検査が FAIL することを
確かめる。一方、論文の**数値**は証明書から生成されるが**定理**は人が書くので、
`test_hand_proofs.py` は証明書を読まずに元データを総当たりして主張を直接照合する。

## 環境

- Python 3.10 + sympy / numpy / networkx (探索側のみ)
- TinyTeX (`lualatex`, jlreq, luatexja, 原ノ味フォント)
- PDF 目視確認に PyMuPDF (`pip install pymupdf`)

## 成果の告知 (`mar announce`)

**成果が出た問題だけ**を X に流す。ここでの成果とは「証明書があり、**その証明書と
今の検証器コードに対する** PASS 記録があり、論文 PDF がビルドされている」もので、
判定は `mar paper --use-record` と同じ条件を使う。探索をやり直して未検証になった
問題は自動的に対象から外れる。

```
python -m mar announce --all              # 未投稿の成果すべての下書きを表示
python -m mar announce p0008_wowii141_girth_tree
python -m mar announce --next --post      # 未投稿のうち 1 件だけ実際に投稿
python tools/announce_schedule.py --at 19:00   # 毎日 1 件流す OS タスクを登録
```

設計原則 5 (数値を直書きしない) は投稿文にも適用する。**投稿文に現れる数はすべて
証明書の `claim` からそのまま引く**。手で書いてよいのは数を含まない見出しだけで、
これはテストで機械的に強制する (`tests/test_announce.py`)。X の 280 単位 (日本語は
1 文字 2 単位、URL は 23 単位) に収まらない分は、**文の途中では切らずに**スレッドの
返信へ回す。同じ成果を二度流さないよう、投稿台帳の鍵は
`(problem_id, certificate_digest)` にしてある — 探索をやり直して結果が変われば
digest が変わるので、「更新版」として投稿できる。

投稿には X API v2 (`tweepy`) と OAuth1 の資格情報 4 つ
(`TWITTER_API_KEY` / `TWITTER_API_SECRET` / `TWITTER_ACCESS_TOKEN` /
`TWITTER_ACCESS_TOKEN_SECRET`) が要る。`.env` 形式で `$MAR_X_ENV` → リポジトリの
`.env.local` → `~/dev/studyai/` の順に探す。**資格情報はリポジトリに置かない**
(`.gitignore` 済み)。API を使わずに予約投稿したい場合は
`~/dev/studyai/tools/x_poster/buffer_auto.py` (Playwright で Buffer に積む) が使える。

## 現在の問題

| ID | 種別 | 内容 | 規模 | 結果 |
|----|------|------|------|------|
| `p0000_jacobian_2026` | 追試 | 2026 年のヤコビアン予想反例の独立検証 + 一般ファイバーの決定 | — | 反例を再現、一般ファイバーを決定 |
| `p0001_txgraffiti_i_mustar` | 未解決 | 正則グラフの独立支配数と飽和数 ($i \le \mu^*$, TxGraffiti 予想 3) | 連結正則グラフ 155,033 個 | 反例なし、等号 3,651 個 |
| `p0002_txgraffiti_zf_alpha` | 未解決 | 亜立方体グラフのゼロ強制数と独立数 ($Z \le \alpha+1$, TxGraffiti 予想 2) | $\Delta \le 3$ ($n \le 10$) + 立方体 ($n \le 18$) 計 48,524 個 | 反例なし、等号 46 個 |
| `p0003_saturation_harmonic` | 追試 + 拡張 | 飽和数と調和指数 ($\mu^* \le H$, TxGraffiti 予想 4, 反証済み) の反例の完全分類 | 連結グラフ $n \le 10$ (11,989,763 個) + 木 $n \le 20$ (1,345,823 個) | 反例ちょうど 145 個を完全分類 (最小は $n = 9$) |
| `p0004_wowii61_induced_forest` | 未解決 | 最大誘導森・残余数・直径 ($f \ge R + \lceil \mathrm{diam}/3 \rceil$, Graffiti.pc WOWII 予想 61) | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | 反例なし、等号 6,854 個を完全分類 (木の等号は星に限る) |
| `p0005_wowii_induced_tree` | 未解決 | 最大誘導木の位数の下界 3 本 (Graffiti.pc WOWII 予想 142・144・146) を 1 つの証人で同時に検証 | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | 反例なし、等号 1,375 / 203 / 13,043 個。予想 142・144 は $\mathrm{girth} \le 3$ または自己中心的なグラフで手証明、木では等号不可能 |
| `p0006_wowii194_hamiltonian` | 未解決 | 独立数と近傍独立数の平均によるハミルトン路の十分条件 ($\alpha \le 1 + l_{\mathrm{avg}}$ ならば traceable, Graffiti.pc WOWII 予想 194) | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | 反例なし。仮定が成り立つのは 3,337,585 個 (24.9%) で、うち 3,322,958 個 (99.6%) は $\alpha \ge 3$ (手証明の射程外)。等号 287,005 個。付随して密度条件 $n < (1+\bar{d})^2$ を手証明し、その系として $n \ge 5$ の木と $n \ge 16$ の 3 正則グラフでは仮定が空虚であることを示した |
| `p0007_wowii200_star_tree` | 反証済み → **反例の無限族 + 最小反例の位数** | 最大誘導木の位数が下界に等しければハミルトン路をもつ ($\mathrm{tree} = \lceil 1 + l_{\mathrm{avg}}\rceil$ ならば traceable, Graffiti.pc WOWII 予想 200。位数 11 の反例が 2026-07-21 に J. Prajapati により公表された) | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 + 反例族 $G_q$ 8 項 | **反例の無限族を構成し、最小反例の位数がちょうど 11 であることを確定した**。星の下界 $\mathrm{tree} \ge \lceil 1 + l_{\mathrm{avg}}\rceil$ (WOWII 予想 140) を手証明して、仮定がその等号成立条件、すなわち「最大誘導木が星で達成され、局所独立数が平均に張り付く」ことと同値であることを示し、この設計図に従って位数 $2q+1$ の族 $G_q$ ($K_q$ から辺 $ab$ を除いた核に、核の全頂点に隣接する非隣接な 2 頂点 $x, y$ と、$a, b$ だけに隣接する $z$ と、各 $c_i$ へのペンダントを足したもの) を作った。$S(G_q) = 4q+4$、閾値も $\mathrm{tree}$ もともに 4、葉がちょうど $q-2$ 枚という閉じた形を手証明したので、$q \ge 5$ では葉が 3 枚以上あってハミルトン路の端点が足りず、**反例は任意に大きい位数で存在する**。公表された反例は $G_5$ に同型 (証明書に置換を記録し、検証器は同型判定を解かずに照合する)。位数 4--10 の全連結グラフ 11,989,760 個に反例が無いことと合わせて最小反例の位数が確定する。仮定を満たす 55,258 個 (0.41%) は graph6 の全リストとして完全分類した (うち 55,249 個は完全グラフでない)。$K_{k,k+1}$ により結論はハミルトン閉路に強化できない。予想 194 の仮定も満たすのは 54,015 個で、残る 1,243 個と $P_4$ により 2 つの仮定クラスが互いに他方を含まないことを示した |
| `p0008_wowii141_girth_tree` | 未解決 → **証明** | 内周と最大誘導木 ($\mathrm{girth}/2 - 1 + l_{\max} \le \mathrm{tree}$, Graffiti.pc WOWII 予想 141) | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | **予想を証明した**。最大次数の頂点を中心とする球 $B_r(v^*)$ ($r = \lfloor \mathrm{girth}/2 \rfloor - 1$) が木を誘導することによる。内周が奇数のときは 1 頂点伸ばせるので、Lean 形式化の切り捨て版より強い有理数のままの形が出る。等号は内周 4 かつ $\mathrm{tree} = 1 + \Delta$ と同値 (定理 3.7)。走査では反例なし、等号 480 個で定理どおり、内周 $\ge 4$ の 20,143 個については全頂点で球が木を誘導することと Moore 型の下界を再計算した |
| `p0009_wowii2_leaf_local_indep` | 未解決 | 葉数と局所独立数の平均 ($2(\overline{\ell} - 1) \le L_s$, Graffiti.pc WOWII 予想 2) | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | 反例なし、等号 13 個を完全分類 ($C_4, \dots, C_{10}$ と $K_{3,3}$, $Q_3$, $K_{4,4}$, $K_{5,5}$, $K_{5,5}-M$, $K_{6,6}-M$)。**二重星定理** (連結グラフの任意の辺 $uv$ で $L_s \ge |N(u) \cup N(v)| - 2$。WOWII 予想 178 の注記にある既知の下界で、本稿では自己完結のため再証明) から NP 困難な $L_s$ を含まない**局所版予想 B'** ($\max_{uv \in E} |N(u) \cup N(v)| \ge 2\overline{\ell}$) と、その強化版**予想 A** ($n \sum_{uv \in E} |N(u) \cup N(v)| \ge 2mS$) を提出した (A $\Rightarrow$ B' $\Rightarrow$ 予想 2)。さらに**共分散定理** ($\mathrm{Cov}(d, \ell) \ge 0$ ならば A、三角形なしなら $\ell = d$ より自動的に成立) を証明。自明帯・$\Delta$ 帯・共分散帯により走査範囲の **13,402,228 / 13,402,242 = 99.9999%** を本稿の定理だけで閉じ、引用した下界を足すと残りはちょうど 1 個。証人は 13,402,070 個 (99.999%) が二重星そのもので、厳密な葉数計算は 13 回だけ |
| `p0010_wowii_ls_lower_bounds` | 未解決 → **反証** | 葉数 $L_s$ の下界予想 11 本 (Graffiti.pc WOWII 予想 154・155・157・160・161・162・165・166・169・171・172) を 1 つの証人で同時に検証 | 連結グラフ $3 \le n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,241 個 ($L_s = n - \gamma_c$ の唯一の例外 $K_2$ を除く) | **予想 172 を反証した**。道 $u_0 \dots u_k$ の両端に葉を 2 枚ずつ付けた木 $T_k$ (位数 $k+5$) は $L_s = 4$ 一定なのに右辺が $G$ 読みで $k-2$、$G^2$ 読みで $\lceil (k-2)/2 \rceil$ と発散し、$k \ge 7$ / $k \ge 11$ で破れる無限族になる ($D(B)$ を最大次数と読んでも $B$ の直径と読んでも反例)。走査でも $G$ 読み 891 個・$G^2$ 読み 16 個の反例を、証人の届かない 1,110 / 25 個について $L_s$ を厳密に最大化して確認した。残る 10 本は反例なしで、新しい道具である**層落差補題** ($L_s \ge \mathrm{drop}(v) = \sum_i (|L_i| - \min(|L_{i+1}|, |L_i \setminus D_1|))$、層幅の下界と球 + ペンダントの下界を同時に含む) により 161・162・157 を手証明し、154・155 は**補題 L1** (ある中心で $|R(v)| = 1$ なら $L_s \ge 1 + \max_u |R(u)|$) の 1 本に還元した。未証明で残るのは L1・160・165・166・169・171 |
| `p0011_wowii_tree_lower_bounds` | 未解決 (部分前進) | 最大誘導木の位数 $\mathrm{tree}(G)$ の下界予想 4 本 (Graffiti.pc WOWII 予想 72・76・84・85。予想 72 は原文の丸め位置が曖昧なので弱い読み 72a と強い読み 72b の両方) を 1 つの証人で同時に検証 | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | 反例なし。等号は 72a が 0 個 (常に真の不等号)、72b が 5,170 個、76 が 1,346,023 個、84 が 3,182 個、85 が 84 個。**測地線補題** ($\mathrm{tree} \ge \mathrm{diam} + 1$) と**星補題** ($\mathrm{tree} \ge \ell_{\max} + 1$) の 2 本だけから 5 つの主張を手証明した: 72a は常に成立し等号を持たない、$\ell_{\max} \le 3$ または $3\,\mathrm{diam} \le 2\ell_{\max} + 3$ なら 72b、$\delta \ge 2$ なら 84 は狭義、$\delta = 1$ でも $\mathrm{diam} \ge 2r - 1$ か $\ell_{\max} \ge 2r - 1$ なら 84、木では 76 が常に等号。これで残余は 72b が 1,487,419 個 (11.1%)、84 が 15,080 個 (0.11%) まで落ちる。残余が硬い理由も具体的なグラフで示した: 2 補題の和 $\mathrm{tree} \ge \mathrm{diam} + \ell_{\max} - 1$ は `DEw` ($C_4$ + 葉) で偽、84 の残余は等号を達成する `F?qb_` ($C_6$ + 葉) を含むので狭義型の議論が通らない。厳密な最大誘導木の計算は 16,254 回 |
| `p0012_wowii19_bipartite_avg_ecc` | 未解決 → **証明** | 二部数 $b(G)$ (誘導部分グラフが二部になる最大位数) の平均離心率による下界 ($b(G) \ge \lfloor \overline{\mathrm{ecc}}(G) \rfloor + \ell_{\max}$, Graffiti.pc WOWII 予想 19。WOWII の $b(G)$ 予想 13--30 で唯一未解決だった 1 本) | 連結グラフ $n \le 10$ + 木 $n \le 20$ + 正則グラフ 9 族 計 13,402,242 個 | **予想を証明した**。**定理 1** ($b(G) \ge \mathrm{ecc}(v) + \ell(v)$。証人は $v$ からの測地線の偶数番目 $\cup \{v\}$ と、$N(v)$ の最大独立集合 $\cup$ 測地線の奇数番目) と**平均離心率補題** ($\overline{\mathrm{ecc}} \le D$ で等号は $r = D$ のときに限る) から、自己中心的 ($r = D$) な場合は定理 1 だけで、$r < D$ の場合は $\lfloor \overline{\mathrm{ecc}} \rfloor \le D - 1$ と既知の予想 13 ($b \ge D + \ell_{\max} - 1$, DeLaViña–Waller 2004) の引用で閉じる。さらに**原文の床関数は天井関数にできない**ことを示した: 星 $K_{1,m}$ ($m \ge 2$) は $\lceil \overline{\mathrm{ecc}} + \ell_{\max} \rceil = m + 2 > m + 1 = b$ で、最小の反例は $P_3$。走査では反例 0、等号 1,116,034 個 (うち天井読みが破れる「平均離心率が非整数」の等号が 331,342 個)、定理 1 だけで閉じず予想 13 の引用が要るのは 646,142 個 (4.8%) で、残り 12,756,100 個 (95.2%) は引用なしで閉じる。厳密な $b(G)$ の計算は 1,169,393 回 |
| `p0013_wowii_ls_plus_b_bounds` | 未解決 → **証明 1 本 + 反証 2 本** | 葉数と二部数の和 $L_s(G) + b(G)$ の下界予想 11 本 (Graffiti.pc WOWII 予想 176--186) を 1 つの証人で同時に検証 | 連結グラフ $n \le 10$ (11,989,763 個) + 木 $11 \le n \le 16$ (32,307 個) + 連結正則グラフ 7 族 (14,577 個) + 二重ほうき木 25 個 + 反例族 $H_t$ 9 個 計 12,036,681 個 | **予想 178 を証明し、予想 176 と予想 181 を反証した**。道具は 2 本だけ: **部分木延長補題** (連結な $S \subsetneq V$ について $L_s \ge |N(S) \setminus S|$。系として $L_s \ge \Delta$ と $L_s \ge \max_e |N(e)| - 2$) と p0012 の**定理 1** ($b \ge \mathrm{ecc}(v) + \ell(v)$)。**予想 178** は $\Delta = n-1$ か否かの場合分けで閉じる (支配的な頂点が無ければどの頂点の離心数も 2 以上、という一行が出題者の注記に残っていた $1$ の差をちょうど埋める)。**予想 176** は二重ほうき木 $D_k$ (道の両端に葉 2 枚ずつ、$n = k+4$) が反例: $L_s + b = n + 4$ 一定なのに右辺は $n + \lceil (k-3)/2 \rceil$ で発散し、$G^2$ 読みで $k \ge 12$、$G$ 読みで $k \ge 8$ から破れて**不足量は非有界**、どちらの読みでも偽。**予想 181** は位数 10・辺数 19 の `` I?q`qhieg `` が走査中で唯一の反例で、これを基礎グラフ $B$ とし $S = \{4,5,6\}$ に全隣接な独立 $t$ 頂点を足した族 $H_t$ ($n = t+10$) が $\alpha = n-6$, $\gamma_c = 4$, $b = n-4$, $\mathrm{diam} = 2$ を満たす**反例の無限族**になる (不足量はどの $t$ でもちょうど 1)。$\mathrm{diam} \le 2$ では $G^2 = K_n$ なので**予想 181 は $\alpha + \gamma_c \le b + 1$ と同値**で、さらに $\gamma_c \le 2$ なら $b \ge \alpha + 1$ から自動的に成立する (反例は $\gamma_c \ge 3$ に限る)。残る 8 本 (177・179・180・182--186) は反例なし。16 通りの**読み**も分類した: 距離を $G$ で測る 184・186 は偽 (反例 31,131 個・79,661 個)、次数を $G$ で測る 181・182 は走査範囲で反例 0 だが**等号も 0 個**で、$G^2$ で測る読み (等号 38,464 個・2,069 個) のほうが sharp。未解決として残るのは観察 **O1** ($L_s + b \ge n + 1$、同値に $b \ge \gamma_c + 1$、同値に $L_s \ge \tau_{\mathrm{oct}} + 1$。走査した全 12,036,681 個で成立、等号は $K_n$ と奇閉路) で、これは定理 1 からは出ない ($C_{2k+1}$ で定理 1 は $b \ge k+2$ しか与えないが必要なのは $b \ge 2k$)。厳密な $L_s$ と $b$ の計算は 1,721,555 回 (14.30%)、木の等式で置き換えたのが 1,142 個 |
| `p0014_wowii_forest_number_bounds` | 未解決 (部分前進) | 森数 $f(G)$ (誘導部分グラフが森になる最大位数) の下界予想 8 本 (Graffiti.pc WOWII 予想 40・58・59・61・63・64・65・66) と、$f$ による二部数 $b(G)$ の上界 1 本 (予想 91) を 1 つの証人で同時に検証 | 連結グラフ $n \le 9$ (273,192 個) + 木 $10 \le n \le 16$ (32,413 個) + $\Delta \le 4$ ($n = 10$, 89,402 個) + 連結正則グラフ 13 族 (25,519 個) 計 420,526 個 + 星 $K_{1,k}$ ($2 \le k \le 40$) 39 個 | **予想 66 の括弧の読みを決定した**。原文の `2*CEIL[em/deg_avg]` は係数 2 が 天井の外とも内とも読めるが、外に読むと星 $K_{1,k}$ ($k$ 偶数) が反例 (最小は $P_3$、走査範囲で 54 個) で、内に読むと同じ族でちょうど等号になる。Graffiti.pc は成立する不等式しか出さないので**原文が意味しているのは内の読み**だと判断できる (同じ曖昧さは予想 53 にもあり、逐語には 50 個の反例)。さらに **$b \le 2f-2$** と 2 つの初等的な補題 ($\alpha < n$ なら $f \ge \alpha+1$ / $f \ge \mathrm{diam}+1$) から**扱う 9 本すべてに仮定つきの証明**を与えた (40・58・59・63・91 が $b \le 2f-2$、61・64・66 が $\alpha$ の補題、65 が測地線の補題): 40 $\Leftarrow p \le 1$、58 $\Leftarrow \ell_{\mathrm{avg}} \ge 2$、59 $\Leftarrow \mathrm{res}\,(2f-2) \le f^2$、61 $\Leftarrow \mathrm{diam} \le 3$、63 $\Leftarrow \mathrm{dist_{even}min} \le f+1$、64 $\Leftarrow \Delta \le \alpha+2$ または $\Delta \mid n$、65 $\Leftarrow \mathrm{dist\,min}(M) \le 3$、66 $\Leftarrow m \ge \mathrm{em}(\Delta+1)$ または $G$ が木、91 $\Leftarrow f(4-L) \le 6$。仮定が被覆するのは 63 が 100%、65 が 99.7%、59 が 97.5% で、最も残るのは 66 (別読み) の 34.7% と 91 の 48.7%。走査範囲では 9 本すべて 反例なし。$f$・$b$・$p$ の決定はどれも NP 困難だが、誘導森・2 彩色・頂点素な奇閉路・道被覆の 4 種の証人で 213,260 個 (50.7%) は厳密解を 計算せずに閉じた |

### 証人つき証明書 (p0002 以降)

$Z$, $\alpha$, $\mu^*$ はいずれも計算が NP 困難なので、「探索器の計算を検証器が
やり直す」設計だと検証が探索と同じだけ高価になる。片側の不等式に限れば、
これは**証人**で回避できる。含意 (「A ならば B」) の場合も、グラフごとに
「結論が真」か「仮定が偽」かのどちらかを証人で示せば、やはり片側で閉じる:

| 主張 | 証人 | 検証コスト |
|------|------|-----------|
| $Z(G) \le \alpha(G)+1$ | 独立集合 $A$ + ゼロ強制集合 $S$ ($|S| \le |A|+1$) | 線形 |
| $\mu^*(G) \le H(G)$ | 極大マッチング $M$ ($|M| \le H(G)$) | 線形 |
| $f(G) \ge R(G) + \lceil \mathrm{diam}(G)/3 \rceil$ | 森を誘導する頂点集合 $F$ ($\lvert F\rvert \ge$ 右辺) | 多項式 (右辺の計算が支配的) |
| 予想 142・144・146 (最大誘導木 $\mathrm{tree}(G)$ の下界 3 本) | 木を誘導する頂点集合 $T$ 1 つ (3 本すべてに同時に効く) | 多項式 (左辺の計算が支配的) |
| 予想 194 ($\alpha \le 1 + l_{\mathrm{avg}}$ ならば traceable) | 含意なので 2 分岐: 結論が真ならハミルトン路 1 本、仮定が偽なら独立集合 $T$ 1 つ | 線形 (どちらの分岐も $\alpha$ を計算しない) |
| 予想 200 ($\mathrm{tree} = \lceil 1 + l_{\mathrm{avg}}\rceil$ ならば traceable) | 同じく 2 分岐: 結論が真ならハミルトン路 1 本、仮定が偽なら位数 $t+1$ の誘導木 1 つ | 線形 (星の下界を手で証明したので、等式の否定が片側に潰れる) |
| 予想 141 ($\mathrm{girth}/2 - 1 + l_{\max} \le \mathrm{tree}$) | 木を誘導する頂点集合 $T$ 1 つ。加えて手証明の各段 (球 $B_r(v)$ が木であること等) を検証器が全頂点で再計算 | 多項式 (証明が付いたので、走査は反例探索ではなく証明の機械照合) |
| 葉数 $L_s$ の下界 11 本 (予想 154–172) | ある全域木の葉集合 $L$ 1 つ (32 ビットマスク)。検証器は補集合が連結支配集合であることだけを見て $L_s \ge \lvert L\rvert$ を得る | 線形 |
| 予想 172 の**反証** | 証人は片側の下界なので反例判定には使えない。証人が右辺に届かなかったグラフに限り $L_s$ を厳密に最大化する (木なら葉を数えるだけ) | 反例候補の個数ぶんだけ NP 困難な計算 |

証人は列挙順に並べたバイナリのサイドカー (`data/witnesses/`) に置き、
その SHA-256 を証明書 JSON に記録する。

逆向きの主張 (反例であること、等号が成立すること、最大値がいくつか) は
片側評価だけでは閉じない。これらは次の 2 つの型で閉じる
(詳細は `docs/methodology.md`):

- **例外リスト方式** — 逆向きが要る対象を漏れなく証明書に列挙し、そこだけ
  検証器が厳密計算する。残りは証人が強い側の不等式 (`|S| <= |A|`) を満たす
  ことで排除する。族の大きさによらず分類全体が閉じる。
- **上下から挟む** — 最大値は「証人による全数の上界」と「達成グラフ 1 個の
  厳密再計算による下界」が一致することで確定する。

網羅性の照合に使う公表値 (OEIS A001349 / A000055 / A002851) は、
探索器の表ではなく検証器が独自にもつ定数を使う。
